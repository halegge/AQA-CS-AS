def primeChecker(n):
    isPrime = False
    for i in range(2, n):
        if n % i == 0:
            isPrime = True
    if isPrime:
        print(str(n) + " is a prime number")
    else:
        print(str(n) + " isn't a prime number")
    
